import 'package:flutter/material.dart';

class DescubriProfileWidget extends StatelessWidget {
  final Color color;

  DescubriProfileWidget(this.color);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: color,
    );
  }
}