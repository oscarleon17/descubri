import 'package:flutter/material.dart';

class DescubriMenuWidget extends StatelessWidget {
  final Color color;

  DescubriMenuWidget(this.color);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: color,
    );
  }
}