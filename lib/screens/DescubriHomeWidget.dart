import 'package:flutter/material.dart';

class DescubriHomeWidget extends StatelessWidget {
  final Color color;

  DescubriHomeWidget(this.color);
  
  @override
  Widget build(BuildContext context) {
    return Container(
      color: color,
    );
  }
}